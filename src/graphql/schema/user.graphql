# import * from "./base.graphql"

type User {
  _id: ID!
  firstName: String!
  lastName: String!
  email: String!
  password: String!
  isConfirmed: Boolean!
  confirmOTP: String
  otpTries: Boolean
  status: Boolean!
}
