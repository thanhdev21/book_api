# import * from "./base.graphql"
# import * from "./user.graphql"

type Mutation {
  createBook(input: CreateBookInput!): Book! @auth(requires: [CLIENT])
}

type Query {
  getAllBooks(pageIndex: Int = 1, pageSize: Int = 20): Books! @auth(requires: [CLIENT])
  getBook(id: ID!): Book! @auth(requires: [CLIENT])
}

type Book {
  _id: ID!
  title: String!
  description: String!
  isbn: String!
  user: User!
  createdAt:Date
  updatedAt:Date
}

type Books {
  items: [Book!]!
  paginate: Paginate
}

input CreateBookInput {
  title: String!
  description: String!
  isbn: String!
}
